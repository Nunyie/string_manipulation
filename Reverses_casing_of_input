# Nunyie
# Reverses the casing for the letters in the input

fullname = input("Please enter your full name in incorrect casing: ")

# Use the swapcase() method to reverse the casing
reversed_casing_fullname = fullname.swapcase()

print(reversed_casing_fullname)
